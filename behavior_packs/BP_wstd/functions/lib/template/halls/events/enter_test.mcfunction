# ===== 玩家进入检测 =====
# 当玩家进入粒子位置后，则触发某些函数

# --- 某关卡 ---
# (指定进入某关卡成功时执行的命令，举例：execute as @a[x=-22,y=9,z=9,r=1] if score progress.easterEgg record matches 0..10 run function levels/.../level_1/start
# (指定进入某关卡失败时执行的命令，举例：execute as @a[x=-22,y=9,z=9,r=1] if score progress.easterEgg record matches -1 run function halls/.../events/level_locked

# --- 某功能 ---
# (指定执行某功能时的命令，举例：execute as @a[x=-22,y=9,z=9,r=1] run function halls/.../functions/...
